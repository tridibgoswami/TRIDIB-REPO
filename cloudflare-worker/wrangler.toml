name = "tradingview-signal-relay"
main = "src/worker.js"
compatibility_date = "2024-01-01"

# Set these via: wrangler secret put WEBHOOK_SECRET
# and:           wrangler secret put EXPO_PUSH_TOKEN
# [vars]         (do NOT put secrets here — use `wrangler secret put`)
